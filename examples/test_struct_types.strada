# test_struct_types.strada - Test Phase 2: Struct types in declarations

struct Point {
    int x;
    int y;
}

struct Person {
    str name;
    int age;
}

func main() int {
    say("Testing struct type system");
    return 0;
}
